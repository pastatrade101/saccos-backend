Database-facing assets live in `supabase/sql` for schema, RLS, and procedure migrations.

Use this folder for backend-side SQL helpers, query templates, or repository utilities that should stay close to the Node service layer without mixing into HTTP modules.
